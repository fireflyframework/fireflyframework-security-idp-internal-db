# Test configuration
spring:
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

logging:
  level:
    org.fireflyframework.security.idp.internaldb: DEBUG
    org.springframework.r2dbc: INFO
    org.flywaydb: INFO
    org.testcontainers: INFO
